# mdTableReader2jsonPrinter

Just read a file that only has .md tables, and return json of data in those tables, kinda via uniq type functionality

```
python ./mdTableReader2jsonPrinter.py <markdownFilename>.md
```
